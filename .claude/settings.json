{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"$(git -C /Users/robertbeliczki/messagingmatrix6 status --porcelain src/ 2>/dev/null)\" ] && [ -z \"$(git -C /Users/robertbeliczki/messagingmatrix6 status --porcelain tasks/todo.md 2>/dev/null)\" ] && echo '{\"systemMessage\":\"⚠️  src/ has uncommitted changes but tasks/todo.md was not updated this session. Update the checkpoint before /clear so the next session can pick up cleanly.\"}' || true"
          }
        ]
      }
    ]
  }
}
