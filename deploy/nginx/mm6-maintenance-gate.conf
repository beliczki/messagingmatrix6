# The gate itself — included inside every location that proxies to the app.
# Needs `set $mm6_tenant <key>;` in the enclosing server block.
if (-f /var/www/mm6-maintenance/$mm6_tenant.deploying) { return 503; }
